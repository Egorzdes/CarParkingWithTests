public class Main {
    @Override
    public int hashCode() {
        return super.hashCode();
    }

    @Override
    public boolean equals(Object obj) {
        return super.equals(obj);
    }

    public static void main(String[] args) {
        //Collection<Car> cars = new ArrayList<>(); // добавление элементов по порядку
        //Collection <Car> cars = new HashSet<>(); // хаотично
        //Collection <Car> cars = new LinkedList<>(); // по порядку
        //Collection <Car> cars = new LinkedHashSet<>(); // по порядку
        //Map <String, Car> carHashMap = new HashMap<>();
//        Set<Integer> carSet = new TreeSet<>(new Comparator<Integer>() {


    }
}